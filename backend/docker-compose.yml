version: "3.8"
services:
  moodcast-backend:
    build: .
    container_name: moodcast-backend
    ports:
      - "8080:8080"
    restart: always
    env_file:
      - .env
    environment:
      - TZ=Asia/Seoul
